{
  "buildCommand": "pnpm install && pnpm --filter @workspace/kings-player run build",
  "outputDirectory": "artifacts/kings-player/dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "memory": 512
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
